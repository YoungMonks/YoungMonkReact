/** * Sample React Native App * https://github.com/facebook/react-native * @flow */import React, { Component } from 'react';import {    Platform,    AppRegistry,    StyleSheet,    Text,    View,    NavigatorIOS,} from 'react-native';import {Navigator} from 'react-native-deprecated-custom-components'import YMLogin from './src/YMLogin'export default class App extends Component<{}> {    _configureScene(route, routeStack) {        return Navigator.SceneConfigs.PushFromRight;    }    _renderScene(route, navigator) {        // 把导航控制器传递给HelloView        // ...route: 获取route中所有属性,传递给HelloView        // ...扩展符, 作用:如果是对象,就获取对象中所有值,如果是数组,就获取数组中所有值        // <route.component navigator={navigator} {...route}/> 类似下面写法，把route的属性取出来赋值        // <route.component navigator={navigator} component=route.component/>        return (<route.component navigator={navigator} {...route.passProps}/>)    }    render(){        return (            <Navigator                style={{flex:1}}                initialRoute={{                    component: YMLogin                }}                configureScene={this._configureScene.bind(this)}                renderScene={this._renderScene.bind(this)}            />            // <NavigatorIOS            //     style={{flex: 1}}            //     initialRoute={{            //         component: YMLogin,            //         navigationBarHidden:true, // 隐藏导航栏            //         title: '登录',            //     }}            // />        );    }}// AppRegistry.registerComponent('App', () => YoungMonkReact);